// This is the content script that runs on web pages.

console.log("MeetAttendify content script loaded!");

// Example: Send a message to the background script
// chrome.runtime.sendMessage({ type: "PAGE_INFO", url: window.location.href }); 